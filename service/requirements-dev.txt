# 只给测试用的依赖。**运行时不依赖任何第三方包**（控制面与 CLI 的服务端全部标准库）——
# 这不是洁癖：一个自己需要一堆依赖的控制面，部署本身就是问题。
#
# 但测试依赖必须显式声明，否则会出这种事（真实发生过）：
# 本机碰巧装了 jsonschema，"测试全绿"；CI 里没装，release 的第一步就红——
# **而 CI 只在打 tag 时跑，所以一直到发布失败才发现。**
pytest>=8
jsonschema>=4
