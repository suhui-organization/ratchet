# Design

> 这份文件记录**已经在跑的世界**，不是愿望清单。它由已上线的站点
> （`web/app/assets/css/main.css`）与本次新增的控制台
> （`service/src/ratchet_service/console/console.html`）反推出来。
> 改动这份文件 = 改动系统，不是记录偏好。

## World

浅色、克制、纸面感。中性灰底 + 一个蓝色作为唯一的强调色（Restrained 策略）。
界面不表演"技术感"：等宽字体只用在**确实是数据**的地方——id、哈希、计数、规则名、时间。
密度靠近工程工具（表格、行、1px 线），不靠近仪表盘（大数字、卡片阵、渐变）。

## Palette

| 令牌 | 值 | 用途 | 对比度（与白底） |
|---|---|---|---|
| `--ink` | `#202124` | 正文、标题 | 16.1:1 |
| `--faint` | `#6b7280` | 次要信息、标签 | 4.83:1 |
| `--line` | `#e5e7eb` | 分隔线、边框 | — |
| `--brand` | `#4d6bfe` | 焦点环 | 非文本，3:1 即可 |
| `--brand-ink` | `#234792` | **主按钮底**、链接 | 8.79:1（白字） |
| `--surface` / `--surface-2` | `#ffffff` / `#f7f8fa` | 页面底 / 行悬停 | — |
| `--code` | `#121c31` | 命令块底 | 13.6:1（浅字） |
| `--pass` | `#12704f` | 通过 | 6.08:1 |
| `--fail` | `#b23c17` | 不通过、断流 | 5.91:1 |
| `--warn-*` | `#8a4b12` / `#fff7ef` / `#eccfae` | 警告块 | 6.4:1 |

**主按钮为什么不用 `--brand`**：`#4d6bfe` 上压白字只有 4.33:1，达不到正文的 4.5:1
（设计检测器也报了同一条）。品牌色继续用于焦点环——那里需要的是 3:1。

## Type

* UI：系统无衬线栈（`ui-sans-serif, system-ui, …`），中英文都用同一套；
* 数据：`JetBrains Mono` → `ui-monospace` 回退；
* 尺度：正文 15px/1.6；标题 16.8px/650；行内数据 13.8px；元信息 13.3px。**没有 display 级字号**——
  这是 Operate 界面，scannability 优先于表达。
* 数字一律 `font-variant-numeric: tabular-nums`（计数、序列号、哈希前缀会对齐）。

## Components

* **行（`.row`）**：可点、可聚焦（`<button>`），悬停换底，选中 `aria-current`。
  桌面左"谁"右"凭什么"两栏；窄屏（≤34rem）自动堆叠——并排会把依据压到读不了。
* **规则条（`ul.rules`）**：九条规则一行一条，左侧是**自绘 SVG 状态点**：
  实心圆=通过、带斜线的圆=不通过、短横=未评估。三个形状不同，不是只靠颜色区分。
* **空状态**：一律给出可执行的下一步（命令），不写"暂无数据"。
* **警告块**：1px 边框 + 暖底 + 自绘三角图标；不用彩色左边框（那是类别默认）。
* **图标**：全部内联 SVG，1.4–1.5px 描边一套；**没有 emoji**。

## Browser surfaces

选中色、插入符、滚动条、焦点环、链接下划线偏移、tabular 数字——都按上面的令牌主题化。
焦点环是 `--brand` 2px + 2px offset。`prefers-reduced-motion` 下所有入场动画关闭。

## Motion

只有一处：列表行出现时 6px 上移 + 淡入，`280ms cubic-bezier(.16,1,.3,1)`，
逐行 40ms 递增。其余一律即时——工具界面里的第二处动画都是干扰。

## Assets

**没有随页面发布的位图**：favicon 是内联 SVG data URI（`#4d6bfe` 底 + 白色棘齿阶梯），
页面不加载任何外部资源（离线内网必须成立）。评审期的截图在 `.impeccable/review/`，
是**评审证据**，不是交付资源。

## Voice in the UI

直说。空状态给命令；失败给原因与恢复动作；"未评估"永远不写成"通过"；
控制面没有认证这件事写在页头，比任何数字都显眼。中文为主，数据里的英文原样保留
（那是凭据里的原文，不该被翻译掉）。
