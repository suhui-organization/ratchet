# 构建上下文 = 仓库根目录，这里排除的东西只影响构建速度与镜像内容，
# 不影响 Git（跟着 .gitignore 走的是另一回事）。
.git/
.github/

# 前端依赖与产物由镜像内的 npm ci / nuxt generate 生成，别从宿主机带进去
web/node_modules/
web/.nuxt/
web/.output/
web/dist/

# 本地构建产物
/bin/
/dist/
/delivery/

# 文档与测试数据不进镜像
docs/
examples/
tests/
**/__pycache__/
*.pyc

# 本地服务器凭据、备份、临时文件
*.db
*.sqlite
*.log
.env
.env.*
tmp/
